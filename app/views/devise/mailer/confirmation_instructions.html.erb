<p><%= t("devise.mailer.welcome", email: @resource.email) %></p>
<p><%= t("devise.mailer.confirmation.request") %></p>
<p><%= link_to t("devise.mailer.confirmation.action"), confirmation_url(@resource, confirmation_token: @token) %></p>
