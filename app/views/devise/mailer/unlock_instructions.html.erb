<p><%= t("devise.mailer.hi", email: @resource.email) %></p>
<p><%= t("devise.mailer.unlock.info") %></p>
<p><%= t("devise.mailer.unlock.request") %></p>
<p><%= link_to t("devise.mailer.unlock.action"), unlock_url(@resource, unlock_token: @token) %></p>
